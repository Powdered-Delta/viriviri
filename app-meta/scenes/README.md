# Meta Spatial Scene Placeholder

This directory is reserved for Spatial Editor source projects such as `Main.metaspatial`.

The exported glXF/runtime assets should land under `app-meta/src/main/assets/scenes/` once the Spatial Editor workflow is connected. The current foundation keeps the file layout in place without committing generated scene output.
