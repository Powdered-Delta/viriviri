# Runtime Scene Assets Placeholder

Spatial Editor exports, including future glXF runtime assets, should land in this directory.

Keep source scene projects under `app-meta/scenes/` and commit generated runtime assets here only when the export pipeline is intentional and reproducible.
